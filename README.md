setting up git
-
```shell
git init -b main
git add .
git commit -m "commit message"
git remote add origin <repo url>  {adding a branch}
git remote rm (branch name)  {to remove branch}
git remote -v {verifies remote branch}
git push origin main
```
